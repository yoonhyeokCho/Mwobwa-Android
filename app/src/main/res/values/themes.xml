<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Mwobwa" parent="Theme.AppCompat.Light">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/app_background_color</item>
        <item name="colorPrimaryVariant">@color/app_background_color</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="windowNoTitle">true</item>
    </style>
    
    <style name="Theme.Mwobwa.splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/app_background_color</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/mwobwa_img</item>
        <item name="postSplashScreenTheme">@style/Theme.Mwobwa</item>
        <item name="windowSplashScreenAnimationDuration">5000</item>
    </style>

    <style name="ColorTextStyle">
        <item name="android:textStyle">bold</item>
        <item name="android:includeFontPadding">false</item>
        <item name="android:textColor">@color/blue_color</item>
        <item name="android:fontFamily">@font/jua</item>
    </style>

    <style name="BlackTextStyle">
        <item name="android:textStyle">bold</item>
        <item name="android:includeFontPadding">false</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:fontFamily">@font/jua</item>
    </style>

    <style name="BtnTextStyle">
        <item name="android:textStyle">bold</item>
        <item name="android:includeFontPadding">false</item>
        <item name="android:textColor">@color/btn_color</item>
    </style>


</resources>